# Copy this to application.properties and fill in real values.
# NEVER commit application.properties with real credentials to git.
server.port=8080

spring.datasource.url=jdbc:mysql://localhost:3306/birthday_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=changeme
spring.jpa.hibernate.ddl-auto=update

mailjet.api.key=
mailjet.api.secret=
mailjet.sender.email=
mailjet.sender.name=Birthday Surprise Bot
mailjet.notify.email=
mailjet.notify.name=Me

upload.dir=uploads
upload.public-base-url=http://localhost:8080/uploads
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

admin.password=

app.cors.allowed-origins=http://localhost:3000,http://localhost:5173
