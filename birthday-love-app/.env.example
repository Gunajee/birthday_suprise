# Copy this file to .env and set your backend URL.
# Leave it empty during local dev if frontend (5173/3000) and backend (8080)
# run on the same machine and you're using a Vite proxy — otherwise set the
# full backend origin here.
VITE_API_BASE_URL=http://localhost:8080
